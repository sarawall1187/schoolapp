<h3><%= @teacher.name %> teaches <%= @teacher.grade_taught.ordinalize %> grade.</h3>

<h4>Children in <%="#{@teacher.name}"%>'s Class: </h4>
<ol>
 <% @teacher.children.each do |child| %>
  <li><%= link_to child.name, child_path(child)  %></li>
 <% end %>
</ol>

<%= button_to 'Back to your Page', parent_path(current_user), method: "get" %>